name: Ultimate Queue And Preview
description: Automatically enables Continue in scene queue and falls back to stream when preview is missing
version: "1.0.0"
ui:
  javascript:
    - UltimateQueueAndPreview.js